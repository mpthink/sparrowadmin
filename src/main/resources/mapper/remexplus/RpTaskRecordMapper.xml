<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.think.sparrowadmin.remexplus.mapper.RpTaskRecordMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.think.sparrowadmin.remexplus.entity.RpTaskRecord">
        <id column="id" property="id"/>
        <result column="task_id" property="taskId"/>
        <result column="gmt_create" property="gmtCreate"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, task_id, gmt_create
    </sql>

    <select id="selectTaskRecords" resultType="java.util.Map">
        SELECT record.id id, task.name name, record.gmt_create gmt_create
        FROM  rp_task_record as record,
              rp_task as task
        where record.task_id = task.id
        order by gmt_create desc
    </select>

</mapper>
